[project]
name = "rbac-poc-fastapi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[all]>=0.116.1",
    "pre-commit>=4.2.0",
    "uvicorn>=0.35.0",
]
